/* eslint object-shorthand:0, prefer-template:0 */
const basedir = __dirname;
const rootdir = '/';

module.exports = {
  basedir: basedir,
  outputdir: basedir + '/public',
  rootdir: rootdir,
  publicPath: rootdir,
};
